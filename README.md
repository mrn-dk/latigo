# latigo
A durable agent harness in Go that compiles to WebAssembly. Sandboxed by construction, reconstructable from its event log, embeddable in any conformant host — with a virtual filesystem, bash-like shell, and capability-based tool calling. Ships with a spec, a reference host, and a conformance suite.
